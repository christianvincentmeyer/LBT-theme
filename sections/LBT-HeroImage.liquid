{%- style -%}
  .LBT_heroImageSection {
    width: 100%;
  }
  .LBT_heroImageSection-content {
  }
  .heroImage {
    display: block !important;
    width: 100%;
    max-width: 1920px;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--Spacing-lg, 24px);
    aspect-ratio: 96/49;
    margin: 0 auto;
    background-image: url('{{ section.settings.image | img_url: "master" }}');
    background-size: 100% auto;
    background-position: center;
    background-repeat: no-repeat;
  }
{%- endstyle -%}

<div class="LBT_heroImageSection color-{{ section.settings.color_scheme }}">
  <div class="LBT_heroImageSection-content">
    <div class="heroImage"></div>
    {% comment %} <img class="heroImage" src="{{ section.settings.image | img_url: "master"}}"> {% endcomment %}
  </div>
</div>

{% schema %}
{
  "name": "LBT HeroImage",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.multicolumn.blocks.column.settings.image.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    }
  ],
  "presets": [
    {
      "name": "LBT HeroImage",
      "category": "section"
    }
  ]
}
{% endschema %}
